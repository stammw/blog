{{#> layout title="Create a new post"}}
{{#*inline "body-block"}}
<form method="POST" id="edit_post"
      {{#if post.id}} action="/post/{{ post.id }}"
      {{else}}        action="/post/new"
      {{/if}}
>
    <div class="form-group">
        <input type="text" name="title" value="{{ post.title }}" />
    </div>
    <div class="form-group">
        <textarea id="edit_post" name="body" rows="10" cols="100" value="">{{ post.body }}</textarea>
    </div>
    <div class="form-group">
        {{#if post.id}}
            <input type="hidden" id="id"
                   name="id"
                   value="{{ post.id }}"
            />
        {{/if}}
        <input type="hidden" id="published"
               value="published"
               {{#if published}}
               checked
               {{/if}}
        />
        <input type="submit" value="Save Draft"/>
        <input type="submit" value="Publish" onsubmit="return publish(this);" />
    </div>
</form>
{{/inline}}
{{/layout}}
