<html>
    <head>
	<title>The make a blog experiment to try rust in web</title>
    </head>
    <body>
	{{#each posts}}
	    <h1><a href="post/{{ id }}">{{ title }}</a></h1>
	    <p>
		{{{ body }}}
	    </p>
	{{/each}}
    </body>
</html>
